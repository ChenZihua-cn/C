/*
给定一个整数数组`nums`和一个整数目标`target`，
请你在该数组中找出和为目标值`target`的那两个整数并返回数组下标。
*/

// 数组不一定是固定长度

// 双指针遍历

#include <iostream>
#include <vector>

using namespace std;

int *twoSum(int *nums){}







/*
int len( int num[] ){

}

int function(int *num, int target){
    int num[] = {1, 2, 3, 4, 5};
    int result[1];
    int target;
    int *flag = &num[0];
    int *probe = flag + 32; // 一个整数是4字节，一字节是8位？
    for(int i = 0; i < len(num); i++){
        for(int j = 0; j < len(num); j++ ){
            if( *flag + *probe == target){
                return result[flag, probe];
            }else{
                break;
            }

        }

    }
    return 0;
}
26.3.8.21:00 utc+8*/